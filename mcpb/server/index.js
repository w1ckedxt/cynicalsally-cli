// Cynical Sally MCP server — launcher placeholder.
//
// The server is actually started via the manifest's mcp_config
// (`npx -y @cynicalsally/cli mcp`), which fetches and runs the published
// npm package. This file exists only to satisfy the MCPB manifest
// `entry_point` requirement; it is not executed at runtime.
